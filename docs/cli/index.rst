CLI Interface
=============
Opaque Client's command line interface requires the ``opaqueclient`` Python package to be installed. Follow the instructions :doc:`here <../install>` to install the Python package if you haven't yet done so. Below are links to CLI usage examples as well as an API reference.

.. toctree::
    usage
    api
